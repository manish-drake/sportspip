import { SportsCheckDirective } from './sports-check.directive';

describe('SportsCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new SportsCheckDirective();
    expect(directive).toBeTruthy();
  });
});
